var stripJsonComments = require('strip-json-comments');

module.exports = function(source) {
  
  this.cacheable && this.cacheable();
  
  try {
    return stripJsonComments(source);
  } catch (err) {
    this.emitError('strip-json-comments-loader: ' + err);
    return source;
  }
  
};
